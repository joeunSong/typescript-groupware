// * basic
//  * install libraries
// * components
import Router from './routes/Index';
// * constants
// * apis
// * utils

const App = (props: any) => {
  const {} = props;

  return <Router />;
};

export default App;
